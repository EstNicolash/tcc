\documentclass[cic,tc]{infufrgs}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{booktabs} 
\usepackage{amsmath} 
%\usepackage{amssymb, amsthm} 
\usepackage{hyperref}

\usepackage[portuguese, ruled, lined]{algorithm2e}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{xcolor}


\lstset{
	basicstyle=\ttfamily\small,
	keywordstyle=\color{blue},
	commentstyle=\color{gray},
	stringstyle=\color{orange},
	breaklines=true,
	frame=single,
	captionpos=b
}


\usepackage{tikz}
\usetikzlibrary{arrows,positioning,shapes}


\newcommand{\sat}[1]{\text{Sat}(#1)}
\newcommand{\ex}{\text{EX}}
\newcommand{\eu}{\text{EU}}
\newcommand{\eg}{\text{EG}}
\def\sectionautorefname{Seção}
\def\chapterautorefname{Capítulo}

\title{Lorem Ipsum}
\translatedtitle{Lorem}

\author{Ipsum}{Lorem} 
\advisor[Prof.~Dr.]{Lorem}{Ipsum} 

\date{Maio}{2026}

\keyword{Verificação de Modelos}
\translatedkeyword{Model Checking}

\begin{document}
	
	\maketitle
	
	\begin{abstract}
		Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.
	\end{abstract}
	
	\begin{translatedabstract}
		Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.
	\end{translatedabstract}
	
	\tableofcontents
	\listoffigures
	\listoftables
	
	\chapter{Introdução}
	
	Este capítulo apresenta a contextualização do trabalho, abordando a relevância da verificação formal, seus objetivos e metodologia.
	
	\section{Motivação}
	
	Um sistema reativo é um programa cujo comportamento não é a mera computação de um função, i.e., um programa que meramente recebe um \textit{input} e retorna um \textit{output}, encerrando-se num estado de terminação. Um sistema reativo, na verdade, é um programa que está em constate execução e sempre interagindo com o seu ambiente. Neste sentido, podemos dizer que um sistema reativo está num ciclo infinito de respostas. Exemplos típicos deste tipo de sistema são: protocolos de comunicação, sistemas embarcados, sistemas operacionais, etc. 
	
	Muitos destes sistemas se enquadram como de missão crítica, ou seja, uma falha de \textit{software} pode causar danos graves, seja para a vida de pessoas, como em programas médicos, ou para a economia \cite{?}. Apesar das dificuldades que o desenvolvimento destes sistemas podem ter, grande parte deles possui um domínio finito de estados, o que permite uma busca exaustiva em todos os estado alcançáveis pelo sistema. Desta forma, é possível verificar com precisão matemática se um sistema possui uma determinada propriedade \cite{?}. Na literatura, esta abordagem exaustiva no espaço de estados se chama \textbf{verificação de modelos} (\textit{model checking}). 
	
	Para isto, um sistema é descrito num sistema de transição o qual será verificado para saber se é um modelo ou não de um fórmula de uma lógica temporal. Uma lógica temporal é um lógica modal onde os operadores modais possuem um interpretação de tempo, sendo ideias para expressar as propriedades de forma precisa de um sistema reativo, o qual justamente executa por um período indeterminado. Especificamente, na verificação de modelos, geralmente estas lógicas apenas lidam com operadores futuros. Como a verificação é feita em todo o espaço de estados, a verificação de modelos possui o problema da \textbf{explosão do espaço de estados}, o que leva a algoritmos buscarem formas de reduzir o espaço, seja por otimização direta em algoritmos explicito ou uma representação simbólica ou abstrata do modelo \cite{??}.
	
	Desde a sua gênese, a área de verificação de modelos vem ganhando cada vez mais espaço em aplicações reais. O maior sucesso da área está no contexto de \textit{hardware}, com ferramentas de verificação sendo essenciais em programas de síntese de circuitos digitais \cite{?}. Ao contrário do caso do \textit{hardware}, há uma recepção menor para \textit{softwares}, principalmente em aplicações já desenvolvidas. 
	Contudo, isto é razão para o progresso da verificação de modelos. Ademais, segundo \citeonline{clarkeIntroductionModelChecking2018}, há três fatos que garantirão o progresso e o sucesso da área, sendo eles: a onipresença de hardwares e softwares só aumenta, evoluindo ainda mais a complexidade da comunicação e se tornando ainda mais presente em missões críticas; há uma mudança na avaliação de qualidade computacional para além do desempenho e do número de funcionalidades, há uma crescente valorização da correção, confiabilidade, segurança e robustez; por fim, os sistemas de transição estados pode ser usado universalmente como ferramenta em outras áreas, como na produção, design, biologia, etc.
	
	
	
	\section{Objetivos e Metodologia}
	Visto a importância da área, o objetivo geral deste trabalho é revisar os principais avanços de verificarão de modelos. 
	Dado que existem diferentes lógicas para descrever as propriedades a serem verificadas, o escopo desta investigação será reduzido à lógica \textit{Computation Tree Logic} (CTL) sem \textit{fairness} e sem a construção de contraexemplos. 
	A revisão foi feita seguindo os principais livros-texto da área. 
	Além disto, os principais algoritmos foram implementados incrementalmente, assim, foi possível averiguar as dificuldades teoricamente apresentadas. 
	Tudo isto fora feito com o intuito de tentar se aproximar do estado da arte, i.e., o que as principais ferramentas acadêmicas e comerciais fazem. 
	
	
	Em virtude disso, o verificador implementado foi comparado com uma ferramenta já consolidada, no caso, NuSMV. E cada passo desenvolvido foi devidamente documentado.
	A comparação serviu para a correção dos algoritmos e também para comparar a eficiência de tempo e memória de uma implementação simples, construída num projeto individual, seguindo como referência o que está disponível em livros-textos. 
	Desta forma, contrastando com uma ferramenta industrial aperfeiçoada por décadas.  
	Para efetuar tal comparação, foi definido um subconjunto da linguagem SMV, a linguagem de descrição do modelo usada no NuSMV, para que ambas as ferramentas processem o mesmo sistema de transições.
	
	\subsection{Implementação}
	NuSMV é implementado em C. Com isto em mente, a implementação do verificador (DAR UM NOME PARA ELE), para ter uma comparação justa, também foi implementado numa linguagem mais próxima ao hardware. No caso, a linguagem Rust foi escolhida por apresentar algumas boas conveniências de desenvolvimento, como seu gerenciador de pacotes \textit{cargo}, garantindo um melhor gerenciamento de dependências e, consequentemente, reprodutibilidade dos experimentos. Além disso, Rust se sobressai por ser uma linguagem moderna e segura no que tange o gerenciamento de memória.
	
	\subsubsection*{Ambiente de Desenvolvimento, Bibliotecas e Reprodutibilidade}
	As bibliotecas e suas versões usadas encontram-se na documentação do projeto nos arquivos \textit{Cargo.toml} e \textit{Cargo.lock}, disponível em \cite{??}.
	O projeto também foi desenvolvido com o uso do gerenciador de pacotes \textit{Nix} e sua funcionalidade experimental \textit{flake}, o que permite a fixação de dependências, permitindo que o projeto tenha uma etapa de \textit{build} consistente e reproduzível em diferentes máquinas (????).
	
	\subsection{Metodologia dos testes}
	Nesta subseção será apresentada a metodologia de teste usada para comparar 
	\subsubsection*{Instâncias}
	Diversas instâncias foram selecionadas para testar o verificador, ver os limites de cada algoritmo. A preferência foi por problemas paramétricos em que é possível averiguar os efeitos num problema de mesma natureza, mas com um crescimento no número de estados.A forma em que cada instância é gerada e quais propriedades são verificadas podem ser vistas na documentação do projeto. Os problemas apresentados são típicos de sistemas reativos: algoritmos de exclusão mútua e protocolos de comunicação, contudo há problemas de outra natureza, como poder ser visto logo abaixo:
	\begin{itemize}
		\item \textit{dining\_N}: o clássico problema do jantar dos filósofos, onde $N$ é o número de filósofos sentados a mesa.
		\item \textit{counter\_N}: um contador binário de $N$ bits. Possui uma estrutura facilmente explorável por algoritmos simbólicos e péssimo para algoritmos explícitos.
		\item \textit{rule30\_N}: um automato celular da regra 30. Um problema de natureza caótica.
		\item \textit{coi\_chaos\_killer\_N}: um problema que necessita de uma otimização de \textit{cone of influence} para poder ser resolvido.
		\item \textit{bad\_order\_N}: o pior caso de um ordenamento de variáveis, usado principalmente para testar a qualidade dos reordenamento em BDDs.  
		\item \textit{brp2\_N}: \textit{Bounded Retransmission Protocol} com $N$ sendo o número de retransmissões.
		\item \textit{elevator2\_N}: um modelo que representa um elevador num edifício de $N$ andares.
		\item \textit{mcs\_N}: o algoritmo de contenção de Mellor-Crummey e Scott com $N$ processos.
		\item \textit{sokoban\_N}: um problema do jogo Sokoban com $N$ caixas.
		\item \textit{fischer\_N}: o algoritmo de exclusão mútua de Fischer com $N$ processos.
		\item \textit{synapse\_N}: protocolo de coerência de cache Synapse com $N$ caches.
		\item \textit{train\_gate\_N}: Protocolo Train-Gate para $N$ nós.
	\end{itemize}
	
	As instâncias \textit{brp2\_N}, \textit{elevator2\_N}, \textit{mcs\_N}, \textit{sokoban\_N}, \textit{fischer\_N}, \textit{synapse\_N} e \textit{train\_gate\_N} foram construídas em cima das instância BEEM disponíveis em \citeonline{ParaDiSe}. 
	A escolha deste conjunto de problemas se deu devido a similaridade razoável dos modelos DVE, presentes no BEEM, com modelos SMV. 
	A conversão de DVE para SMV foi feita com IA. Não se garante a correção perfeita dos modelos, porém, ao menos, as especificações são corretamente verificadas em todos os casos.
	\subsubsection*{Dados coletados}
	Para cada execução de um modelo SMV, os seguinte dados foram coletados:
	\begin{itemize}
		\item \textbf{Tempo de Compilação:} o tempo \textit{parsing} do SMV e construção do BDD ou da estrutura de Kripke explícita.
		\item \textbf{Tempo de Verificação:} o tempo para verificar todas as propriedades CTL no modelo.
		\item \textbf{Tempo Total:} o tempo total de execução (compilação+verificação).
		\item \textbf{Nós Estáticos:} o número de nós do BDD após a compilação.
		\item \textbf{Nós de Verificação:} o número de nós de BDD após a verificação.
		\item \textbf{Pico de Memória:} o pico de memória usado durante a execução, coletada com a ferramenta \textit{time} do Linux.
		\item \textbf{Número de estados:} o número de estados exploráveis pelo algorítimo explícito. 
	\end{itemize}
	\subsubsection*{Execuções}
	São 30 execuções para cada configuração de instância e heurística de ordenamento de variáveis. 
	No caso da verificação em BDD, o NUSMV e o () são executados de forma paralela. 
	Há um tempo limite estático de 60 segundos para uma execução. 
	Porém, há também um tempo limite dinâmico, o qual serve para encerrar o processo competidor caso ele demore muito para terminar sua execução, ou seja, assumimos como não sendo competitivo. 
	Este tempo limite dinâmico é definido pela função $f(t) = t + 10\sqrt{t}+5$ onde $t$ é tempo de execução do primeiro programa (NuSMV ou ()) que terminou, ou seja, caso NuSMV tenha terminado no tempo $t$, () tem até o tempo $f(t)$ para terminar.
	
	\subsubsection*{Visualização e teste}
	Foram produzidos as seguintes visualizações:\\
	Foram usados os seguintes testes estatísticos.\\
	
	
	
	
	\subsection{Uso de Inteligência Artificial Generativa}
	Inteligências artificiais generativas foram usadas no desenvolvimento do verificador. O seu uso principal foi em código não relevante para a compreensão dos algoritmos, como no desenvolvimento do \textit{parsing}, no gerador de instâncias, na produção de testes unitários, na refatoração de código, na interface de linha de comando e na documentação inicial. Os modelos usados foram \textbf{Gemini 3.1} e \textbf{Claude Sonnet 4.6}.
	
	
	
	
	\chapter{Fundamentação Teórica}
	

	Um sistema deve ser descrito seguindo um formalismo específico onde suas componentes e comportamentos principias são abstraídas numa estrutura matemática. O formalismo é a semântica de Kripke apresentado na \autoref{sec:estruturas_de_kripke}. Segundo \citeonline{baierPrinciplesModelChecking2008a}, as propriedades de sistemas reativos são de dois tipos: propriedades de segurança (\textit{safety}) e de vivacidade (\textit{liveness}). Informalmente, as propriedades de segurança garantem que algo de ruim numa aconteça. Um exemplo mais simples deste tipo é verificação de invariantes. Já as propriedades de vivacidade garantem que algo de bom aconteça. Qualquer outra propriedade é uma composição de propriedades destes dois tipos. Estas propriedades são formalizadas por meio de lógicas temporais. A lógica temporal estudada neste trabalho é apresentada na \autoref{sec:ctl}. O formalismo apresentado segue 
	as principalmente as construções de \citeonline{blackburnModalLogic2010} e \citeonline{baierPrinciplesModelChecking2008a}.
	
	\section{Estruturas de Kripke}
	\label{sec:estruturas_de_kripke}
	
	A semântica de mundos possíveis (ou semântica de Kripke) é o formalismo padrão para lógicas modais \cite{fittingFirstOrderModalLogic2023}. Um \textit{\textbf{frame}} é um par ordenado $(S, R)$ onde:   
		\begin{itemize}
		\item $S$ é um conjunto finito de \textbf{objetos} (mundos possíveis, estados, etc.).
		\item $R \subseteq S \times S$ é uma \textbf{relação de acessibilidade}.
	\end{itemize}
	\par
	Lê-se $(s,s')\in R$ como $s'$ é acessível pelo mundo $s$. Um \textit{frame} pode ser facilmente visualizado como um dígrafo. Um \textbf{modelo} $M$ é uma tupla $(F, AP, L)$ em que:
		\begin{itemize}
		\item $F$ é um \textit{frame}.
		\item $AP$ é um conjunto finito de \textbf{proposições atômicas}.
		\item $L: S \to 2^{AP}$ é uma \textbf{função de rotulação} que associa a cada estado um conjunto de proposições que nele são verdadeiras ($M,s\models p\iff p\in L(s)$).
	\end{itemize}
	
	\par
	No contexto de verificação de modelos, um sistema reativo é modelado para esta semântica, com o adicional do conjunto $I$ de \textbf{estados iniciais} tal que $I \subseteq S$ para capturar o equivalente de \textbf{sistemas de transição}. Em \citeonline{clarkeHandbookModelChecking2018} e \citeonline{clarkeModelChecking2018}, a tupla $(M,I)$ são chamadas de \textbf{estruturas de Kripke}. No caso da \textit{Computation Tree Logic} (CTL), há o adicional de a relação de acessibilidade $R$ necessita ser total, i.e.,  $\forall_s\exists_{s'}((s,s')\in R)$ onde $s,s'\in S$. 
	
	\section{Computation Tree Logic (CTL)}
	\label{sec:ctl}
	Seguindo a formalização de \cite{baierPrinciplesModelChecking2008a}.
	
	\subsection{Interpretação do tempo}
	Uma lógica temporal necessita de uma interpretação específica. 
	Uma interpretação possível é a de tempo linear em que o tempo é visto como uma sequência de acontecimentos únicos, possuindo sempre um possível resultado a cada ponto. 
	Esta interpretação é usada na \textit{Linear Time Logic} (LTL).
	Outra interpretação possível é ver o tempo como uma ramificação de todas possibilidades possíveis num determinado ponto. 
	Esta visão permite uma noção de \textbf{caminhos}, os quais alguns podem satisfazer determinada propriedade ou não.
	Dado uma estrutura de Kripke $(M,I)$, a \textbf{árvore de computação}, a estrutura que representa todas os caminhos possíveis, é obtida pelo \textit{unfolding} de $M$ tendo como raiz um estado de $I$ \cite{baierPrinciplesModelChecking2008a}. 
	A lógica de árvore de computação (\textit{Computation Tree Logic}) trabalha com esta interpretação temporal.
	
	\subsection{Sintaxe}
	
	A gramática das fórmulas de CTL sobre um conjunto $AP$ de proposições atômicas é definida através do seguinte conjunto de operadores:
	
	\begin{equation}
		\Phi ::= \top \mid a \mid \neg \Phi \mid \Phi_1 \land \Phi_2 \mid EX \Phi \mid E[\Phi_1 U \Phi_2] \mid EG \Phi
	\end{equation}
	
	Onde $a \in AP$ representa uma proposição atômica. A partir deste conjunto, definimos os demais operadores como abreviações lógicas:
	\begin{equation}
		\begin{aligned}
			AX \Phi &\equiv \neg EX \neg \Phi \\
			EF \Phi &\equiv E[\top U \Phi] \\
			AF \Phi &\equiv \neg EG \neg \Phi \\
			AG \Phi &\equiv \neg EF \neg \Phi \\
			A[\Phi_1 U \Phi_2] &\equiv \neg (E[\neg \Phi_2 U (\neg \Phi_1 \land \neg \Phi_2)] \lor EG \neg \Phi_2)
		\end{aligned}
	\end{equation}


	\subsection{Semântica}
	Seja $a$ um proposição atômica de $AP$, $\Phi$ uma formula CTL, um \textit{frame} $F = (S,R)$ e um modelos $M = (F, AP, L)$. Definimos um caminho como uma tupla infinita de estados $(s_0,s_1,s_2,...)$. A satisfação ($\models$) de um formula num estado $s_0\in S$ do modelo $M$ é definido da seguinte forma:
	\begin{itemize}
		\item $M, s_0 \models a \iff a \in L(s_0)$
		\item $M, s_0 \models \neg \Phi \iff M, s_0 \not\models \Phi$
		\item $M, s_0 \models \Phi_1 \land \Phi_2 \iff M, s_0 \models \Phi_1 \text{ e } M, s_0 \models \Phi_2$
		\item $M, s_0 \models EX \Phi \iff \exists \pi = (s_0, s_1, \dots) \text{ tal que } M, s_1 \models \Phi$
		\item $M, s_0 \models EG \Phi \iff \exists \pi = (s_0, s_1, \dots) \text{ tal que } \forall i \geq 0, M, s_i \models \Phi$
		\item $M, s_0 \models E[\Phi_1 U \Phi_2] \iff \exists \pi = (s_0, s_1, \dots) \text{ tal que } \exists j \geq 0 \mid (M, s_j \models \Phi_2 \land \forall k < j, M, s_k \models \Phi_1)$
	\end{itemize}
	As abreviações (2.2) podem ser definidas independentemente, para melhor compreensão destas, da seguinte forma:
	\begin{itemize}
		\item  $M, s_0 \models AX \Phi \iff \forall \pi = (s_0, s_1, \dots) \mid M, s_1 \models \Phi$
		\item  $M, s_0 \models EF \Phi \iff \exists \pi = (s_0, s_1, \dots), \exists i \ge 0 \mid M, s_i \models \Phi$
		\item  $M, s_0 \models AF \Phi \iff \forall \pi = (s_0, s_1, \dots), \exists i \ge 0 \mid M, s_i \models \Phi$
		\item  $M, s_0 \models AG \Phi \iff \forall \pi = (s_0, s_1, \dots) \mid \forall i \ge 0, M, s_i \models \Phi$
		\item  $M, s_0 \models A[\Phi_1 U \Phi_2] \iff \forall \pi, \exists j \ge 0 \mid (M, s_j \models \Phi_2 \land \forall k < j, M, s_k \models \Phi_1)$
	\end{itemize}
	
	\subsection*{Compreensão informal dos operadores temporais}
	Cada operador temporal pode ser lido informalmente assim:
	\begin{itemize}
		\item \textbf{$EX\phi$:} $\Phi$ é satisfeita no próximo estado de algum caminho.
		\item \textbf{$AX\phi$:} $\Phi$ é satisfeita no próximo estado de todos caminhos possíveis.
		\item \textbf{$EG\phi$:} $\Phi$ é satisfeita no em todos estados de algum caminho.
		\item \textbf{$AG\phi$:} $\Phi$ é satisfeita no em todos estados de todos caminho possíveis.
		\item \textbf{$EF\phi$:} $\Phi$ é eventualmente satisfeita em algum estado de algum caminho.
		\item \textbf{$AF\phi$:} $\Phi$ é eventualmente satisfeita em algum estado de todos caminhos possíveis.
		\item \textbf{$E[\Phi_1 U \Phi_2]$:} $\Phi_1$ é satisfeita até que $\Phi_2$ é satisfeita em algum caminho.
		\item \textbf{$A[\Phi_1 U \Phi_2]$:} $\Phi_1$ é satisfeita até que $\Phi_2$ é satisfeita em todos os caminhos possíveis.
	\end{itemize}
	
	
	\subsection{Propriedade de ponto fixo}
	REMOVER?
	
	\chapter{SMV, NuSMV e SSMV}
	A razão pela opção de comparar com o NuSMV se dá pelo fato de o SMV ser pioneiro na verificação simbólica de modelos. 
	Além disso, o SMV foi concebido inicialmente para a lógica CTL, sendo então facilmente conversível para as representações em BDD ou simbólica para esta lógica, a qual define o escopo deste trabalho.
	
	
	Mais especificamente, este trabalho faz a comparação com a reimplementação NuSMV. 
	Há razões de interesse prático para esta escolha: 
	Em primeiro lugar, possui código aberto, permitindo uma análise mais minuciosa das diferenças com a implementação de (). 
	Em segundo lugar, o NuSMV atualmente ainda é relevante. 
	Por exemplo, SMV é a linguagem de descrição (modelagem) e o NuSMV é um dos núcleos do nuXmv, o qual tem uma boa participação recente em uma competição de verificação de modelos em hardware \cite{HWMCC25} e um uso industrial relevante para um design da NASA \cite{rozierMoXIIntermediateLanguage2025}.
	
	Para evitar trabalho desnecessário aos propósitos desta pesquisa, reduzimos a linguagem SMV no verificador () para um subconjunto (SSMV, de \textit{Subset} SMV) contendo apenas as estruturas de dados primitivas (inteiros, enumerações e boolianos) e um único módulo (MAIN), sem especificações LTL e sem diretivas específicas de FAIRNESS, TRANSITION, etc.
	A gramática SSMV foi construída de forma mais restrita para simplicidade de implementação, o que leva à sua não caracterização em si como um subconjunto da linguagem SMV. Entretanto, as instâncias foram construídas de um modo que ambos os verificadores consigam compreender e reproduzir os mesmos resultados.
		
	
	
	
\chapter{Verificação de modelos explícita}

Neste capítulo, será apresentada a verificação de modelos explícita, na qual o modelo SMV é representado em uma estrutura de Kripke. Os resultados da implementação desse algoritmo serão analisados conjuntamente com a comparação entre os motores simbólicos a partir do Capítulo \ref{verificacao_simbolica}.


\section{Algoritmo de rotulação (\textit{labelling})}
O algoritmo para verificação explícita implementado foi o algoritmo de rotulação, apresentado primeiramente no trabalho seminal de \citeonline{clarkeDesignSynthesisSynchronization1982}. 
A formulação usada como referência inicial foi a de \citeonline{huthLogicComputerScience2000}. 
A implementação em Rust pode ser acessada no código-fonte disponível em \citeonline{??}.

\section{Notas de Implementação}
Para evitar ao máximo o desperdício de memória desse algoritmo, algumas escolhas cruciais de estruturas de dados foram feitas seguindo o paradigma \textit{Data-Oriented Design} (DOD). 
Como resultado, houve uma redução de aproximadamente 70\% no uso de memória em algumas execuções. 
As principais razões para tal otimização foram:
\begin{itemize}
	\item Uso de arenas, que são vetores unidimensionais, para armazenar os dados. Qualquer referência aos dados é feita por um identificador único, o qual representa a posição da arena na qual ele se encontra.
	\item Com essa estrutura de dados, é possível implementar um mecanismo para evitar a duplicação de dados. Por exemplo, uma mesma proposição não precisa ser alocada mais de uma vez na memória, mesmo que apareça repetidamente na descrição ou na especificação.
	\item O \textit{frame} de transições é representado por uma matriz CSR (\textit{Compressed Sparse Row}), ideal para grafos esparsos e exploração de localidade na memória.
	\item Os conjuntos de estados são representados internamente por \textit{bitsets}, permitindo que as operações de conjuntos sejam executadas bit a bit (\textit{bitwise}).
\end{itemize}     

\section{O problema da explosão de estados}

O principal problema da verificação explícita, principalmente do algoritmo mais ingênuo de rotulagem, é que ela sofre por completo com o maior desafio da verificação de modelos: a explosão de estados \cite{??}. 
Isso pode ser exemplificado de forma simples: seja um modelo $M$ com $N$ variáveis boolianas; assumindo que todas as combinações de valores sejam acessíveis, teremos ao final $2^N$ estados possíveis.
Assim, o obstáculo do algoritmo de rotulação surge principalmente no tamanho do modelo, dada sua complexidade pseudopolinomial $O(|M||f|)$, onde $|M|$ é o tamanho do modelo (a soma do número de estados e do número de transições) e $|f|$ é o tamanho da fórmula \cite{baierPrinciplesModelChecking2008a}.
O grande movimento dos pesquisadores, então, consiste em encontrar formas de reduzir o tamanho da representação do espaço de estados, construindo representações simbólicas ou abstratas \cite{clarkeIntroductionModelChecking2018}.
Nos capítulos a seguir, será apresentada a principal representação simbólica usada para a lógica CTL.



\begin{figure}[H]
	\centering
	% Cria um bloco preto de 10cm de largura por 6cm de altura como placeholder
	\rule{10cm}{6cm} 
	\caption{Gráfico com explosão de estados.}
	\label{fig:exemplo_explosao}
\end{figure}



\chapter{Binary Decision Diagrams}
	
	
	
	\chapter{Binary Decision Diagrams}
	(REDUZIR)
	\section{\textit{Switching Functions}}
	A verificação simbólica opera sobre conjuntos de estados representados por funções boolianas.
	
	\subsection*{Variáveis e avaliações:}
	Seja um conjunto de variáveis boolianas $Var = \{x_1,x_2,x_3,...,x_n\}$. Uma \textbf{avaliação} (do conjunto) é uma atribuição de valores boolianos (0 ou 1) às variáveis (do conjunto), ou seja, uma avaliação é um função $\eta: Var\mapsto \{0,1\}$. $Eval(Var)$ é o conjunto de \textbf{todas as avaliações (do conjunto de variáveis)}.
	
	\subsection*{\textit{Switching Functions}}
	Uma \textbf{\textit{switching function}} é uma função $f: Eval(Var) \mapsto \{0,1\}$. No contexto de verificação, a \textbf{função característica} $\chi_T$ para representar um subconjunto de estados $T \subseteq S$:
	\begin{equation}
		\chi_T(s) = 1 \iff s \in T
	\end{equation}
	Desta forma, operações sobre conjuntos (união, interseção, etc) são realizadas por operações lógicas ($\lor$,$\land$, etc.) sobre \textbf{switching functions}.  
	
	\subsection*{Cofatores}
	Um cofator de uma função $f$ é como fixasse uma das variáveis com um valor booliano. Seja $f:Eval(y,x_1,x_2,...,x_n)\to\{0,1\}$,  cofator positivo é $f|_{x=1}:Eval(1,x_1,x_2,...,x_n)\to\{0,1\}$ e o cofator negativo é $f|_{x=0}:Eval(0,x_1,x_2,...,x_n)\to\{0,1\}$. Caso $f|_{x=1}\neq f|_{x=0}$, $x$ é considerado uma \textbf{variável essencial}.
	
	\subsection*{Quantificadores}
	\begin{equation}
		\exists x.f = f|_{x=0}\lor f |_{x=1}
	\end{equation}
	\begin{equation}
		\forall x.f = f|_{x=0}\land f |_{x=1}\\
	\end{equation}
	
	\subsection*{Operação de renomeação}
	Exemplo: seja $f = x \land(\neg x\lor y)$. Renomeando $x$ para $x'$ temos:
	$f\{x\gets x'\} = x' \land(\neg x'\lor y)$.
	
	\section{Binary Decision Trees}
	\section{Binary Decision Diagram}
	
	Seja um conjunto de variáveis $Var =\{x_1,x_2,...,x_n\}$. Um \textit{Binary Decision Diagram} é uma tupla $(V,V_I,V_T,succ_0,succ_1,var,val,v_0)$ em que temos:
	\begin{itemize}
		\item Um conjunto finito de nós $V$.
		\item Os conjuntos $V_I\subset V$ e $V_T\subset V$ tal que $V_I$ são os nós internos, $V_T$ são nós terminais e $V_I\cap V_T=\emptyset$.
		\item As funções de sucessores $succ_0: V_I\to V$ e  $succ_1: V_I\to V$.
		\item A função de atribuição de nós internos a variáveis $var: V_I\to Var$.
		\item A função de atribuição de valores boolianos a nós terminais $val: V_T\to \{0,1\}$.
		\item Um nó raiz $v_0$ em que $\neg\exists x(succ_1(x)=v_0\lor succ_0(x)=v_0)$.
	\end{itemize}
	
	\textbf{Exemplo 1: Árvore de Decisão}\\
	Uma árvore de decisão binária é um caso particular de BDD. Abaixo, a representação da função $f = x_1 \land x_2$:
	
	\begin{center}
		\begin{tikzpicture}[node distance=1.5cm, >=stealth, bend angle=45, auto]
			% Nós Internos
			\node[circle,draw] (v0) {$x_1$};
			\node[circle,draw, below left=1cm and 0.5cm of v0] (v1) {$x_2$};
			\node[circle,draw, below right=1cm and 0.5cm of v0] (v2) {$x_2$};
			
			% Nós Terminais
			\node[rectangle,draw, below left=1cm and 0.2cm of v1] (t0) {0};
			\node[rectangle,draw, below right=1cm and 0.2cm of v1] (t1) {0};
			\node[rectangle,draw, below left=1cm and 0.2cm of v2] (t2) {0};
			\node[rectangle,draw, below right=1cm and 0.2cm of v2] (t3) {1};
			
			% Arestas (Dashed = 0, Solid = 1)
			\draw[->, dashed] (v0) -- (v1);
			\draw[->] (v0) -- (v2);
			
			\draw[->, dashed] (v1) -- (t0);
			\draw[->] (v1) -- (t1);
			
			\draw[->, dashed] (v2) -- (t2);
			\draw[->] (v2) -- (t3);
		\end{tikzpicture}
	\end{center}
	
	\textbf{Exemplo 2: BDD Não Ordenado}\\
	Em um BDD não ordenado para a função $f = x\lor y$
	
	\begin{center}
		\begin{tikzpicture}[
			node distance=1.8cm and 2.5cm,
			v/.style={circle, draw, minimum size=8mm},
			t/.style={rectangle, draw, minimum size=6mm},
			>=stealth,
			thick
			]
			
			% Camada 1: Raiz
			\node[v] (r) {$x$};
			
			% Camada 2: y aparece em ambos os ramos
			\node[v] (yL) [below left=of r] {$y$};
			\node[v] (yR) [below right=of r] {$y$};
			
			% Camada 3: O "problema" - x aparece DEPOIS de y no ramo da direita
			\node[v] (x_down) [below=of yR] {$x$};
			
			% Camada 4: Terminais alinhados na base
			% Posicionados de forma absoluta ou relativa ao nó mais baixo
			\node[t] (t0) [below left=2cm and 1cm of yL] {0};
			\node[t] (t1) [below right=2cm and 1cm of yR] {1};
			
			% --- Conexões Ramo Esquerdo (x=0 -> y) ---
			\draw[->, dashed] (r) -- (yL);
			\draw[->, dashed] (yL) -- (t0);
			\draw[->] (yL) -- (t1);
			
			% --- Conexões Ramo Direito (x=1 -> y -> x) ---
			\draw[->] (r) -- (yR);
			
			% y_right com y=0 vai para o x_down (violação de ordem!)
			\draw[->, dashed] (yR) -- (x_down);
			
			% y_right com y=1 vai direto para o terminal 1
			\draw[->] (yR) -- (t1.north west);
			
			% x_down (o x que aparece tarde demais)
			\draw[->, dashed] (x_down) -- (t0);
			\draw[->] (x_down) -- (t1);
			
		\end{tikzpicture}
	\end{center}
	
	\textbf{Exemplo 2: BDD Ordenado mas não Reduzido}\\
	Neste exemplo da função $f = x \lor y$, os nós terminais 0 e 1 são compartilhados.
	\begin{center}
		\begin{tikzpicture}[node distance=2cm, >=stealth, auto]
			% Nós Internos
			\node[circle,draw] (vx) {$x$};
			\node[circle,draw, below left of=vx] (vy0) {$y$};
			\node[circle,draw, below right of=vx] (vy1) {$y$};
			
			% Nós Terminais únicos
			\node[rectangle,draw, below=3cm of vx] (t0) {0};
			\node[rectangle,draw, right=1.5cm of t0] (t1) {1};
			
			% Arestas de X
			\draw[->, dashed] (vx) -- (vy0);
			\draw[->] (vx) -- (vy1);
			
			% Arestas de vy0 (Caminho onde x=0, então f depende de y)
			\draw[->, dashed] (vy0) -- (t0);
			\draw[->] (vy0) -- (t1);
			
			% Arestas de vy1 (Caminho onde x=1, então f é sempre 1)
			% Note que vy1 é um nó redundante: ambos os caminhos levam a 1
			\draw[->, dashed] (vy1) -- (t1);
			\draw[->] (vy1) -- (t1);
		\end{tikzpicture}
	\end{center}
	
	\section{Ordered Binary Decision Diagram (OBDD)}
	Um $\mathcal{P}$-OBDD é um BDD $\mathcal{B}$ que respeita um ordenamento de variáveis $(x_1,x_2,...,x_n)$ em que $x_i<x_{i+1}$ para qualquer valor de $i\in{\{1,...,n\}}$. A função de sucessores deve ser consistente com a relação de ordem, ou seja, se $succ(v_i) = v_j$, então, $var(v_i)<var(v_j)$. \\ 
	
	\textbf{Exemplo 1: BDD Ordenado (mas não Reduzido)}\\
	Neste exemplo da função $f = x \lor y$. Os nós terminais 0 e 1 são compartilhados.
	\begin{center}
		\begin{tikzpicture}[node distance=2cm, >=stealth, auto]
			% Nós Internos
			\node[circle,draw] (vx) {$x$};
			\node[circle,draw, below left of=vx] (vy0) {$y$};
			\node[circle,draw, below right of=vx] (vy1) {$y$};
			
			% Nós Terminais únicos
			\node[rectangle,draw, below=3cm of vx] (t0) {0};
			\node[rectangle,draw, right=1.5cm of t0] (t1) {1};
			
			% Arestas de X
			\draw[->, dashed] (vx) -- (vy0);
			\draw[->] (vx) -- (vy1);
			
			% Arestas de vy0 (Caminho onde x=0, então f depende de y)
			\draw[->, dashed] (vy0) -- (t0);
			\draw[->] (vy0) -- (t1);
			
			% Arestas de vy1 (Caminho onde x=1, então f é sempre 1)
			% Note que vy1 é um nó redundante: ambos os caminhos levam a 1
			\draw[->, dashed] (vy1) -- (t1);
			\draw[->] (vy1) -- (t1);
		\end{tikzpicture}
	\end{center}
	
	\section{Reduced Ordered Binary Decision Diagram (ROBDD)}
	Um ROBDD é um $\mathcal{P}$-OBDD em que $\forall v,w\in V(v\neq w\to f_v \neq f_w)$. $f_v$ é a função induzida de um BDD tomando o nó $v$ como raiz. \\
	
	\subsection*{Exemplo: Função e BDD induzido}
	Seja o BDD para $f = x \land y$. Cada nó $v$ induz uma função $f_v$ baseada em sua posição na estrutura:
	
	\begin{center}
		\begin{tikzpicture}[node distance=1.5cm, v/.style={circle,draw,minimum size=8mm}, t/.style={rectangle,draw,minimum size=6mm}, >=stealth, thick]
			\node[v] (vx) {$x$};
			\node[v, below right=of vx] (vy) {$y$};
			\node[t, below left=of vx] (t0) {0};
			\node[t, below=of vy] (t1) {1};
			
			\draw[->, dashed] (vx) -- (t0);
			\draw[->] (vx) -- (vy);
			\draw[->, dashed] (vy) -- (t0);
			\draw[->] (vy) -- (t1);
			
			% Notas explicativas
			\node[right=0.5cm of vx, blue] {$f_{vx} = x \land y$};
			\node[right=0.5cm of vy, blue] {$f_{vy} = y$};
			\node[left=0.5cm of t0, blue] {$f_{t0} = 0$};
		\end{tikzpicture}
	\end{center}
	Note que a função induzida pela raiz é a própria função alvo, enquanto o nó $y$ induz a função correspondente ao seu valor de verdade, já que $x$ foi avaliado como 1.
	
	\subsection*{Regras de redução:}
	ROBDD é a forma canônica de um OBDD, ou seja, todo OBDD pode ser transformado num ROBDD por meio de uma transoformação. Dado um $\mathcal{B}=(V,V_I,V_T,succ_0,succ_1,var,val,v_0)$, as regras de redução são:
	\begin{itemize}
		\item \textbf{Regra de eliminação:} Se $v\in V_I$ com $succ_0(v)= succ_1(v)=w$, então, $v$ deve ser removido e, todo $u$, tal que $succ_1(u) = v$, será redirecionado para $w$,  $succ_1(u) = w$. O mesmo se aplica para o caso em que $succ_0(u)=v$.
		\item \textbf{Regra de isomorfismo:} Se $v,w\in V$, $v\neq w$ e $v,w\in V_T$ atribuídos a um mesmo valor ($val(v)=val(w)$) ou $v,w\in V_I$ com $(var(v),succ_1(v),succ_0(v))=(var(w),succ_1(w),succ_0(w))$, então remove $v$ e redireciona os nós $u$ em que $succ_1(u) = v$ para $w$ ($succ_1(u) = w$). O mesmo se aplica para $succ_0(u)=v$.
	\end{itemize}
	
	\subsection*{Exemplo: redução passo a passo}
	Partimos de um OBDD para $f = x \lor y$ que contém um nó redundante e terminais duplicados.
	
	\textbf{Passo 1: Regra de Isomorfismo (Terminais)} \\
	Agrupamos todos os nós terminais que possuem o mesmo valor. Haverá sempre um único nó terminal por valor.
	
	\begin{center}
		\begin{tikzpicture}[
			node distance=1.2cm and 1.5cm,
			v/.style={circle, draw, minimum size=7mm, inner sep=0pt},
			t/.style={rectangle, draw, minimum size=6mm},
			>=stealth, thick, scale=0.9, every node/.style={transform shape}
			]
			
			% --- LADO ESQUERDA: ÁRVORE COMPLETA ---
			\node (labelTree) at (-2, 0.8) {\textbf{1. Árvore de Decisão}};
			\node[v] (x1) at (-2,0) {$x$};
			\node[v] (yL1) at (-3,-1.5) {$y$};
			\node[v] (yR1) at (-1,-1.5) {$y$};
			
			% Terminais da Árvore (4 nós)
			\node[t] (t0a) at (-3.7,-3) {0};
			\node[t] (t1a) at (-2.7,-3) {1};
			\node[t] (t1b) at (-1.3,-3) {1};
			\node[t] (t1c) at (-0.3,-3) {1};
			
			\draw[->, dashed] (x1) -- (yL1); \draw[->] (x1) -- (yR1);
			\draw[->, dashed] (yL1) -- (t0a); \draw[->] (yL1) -- (t1a);
			\draw[->, dashed] (yR1) -- (t1b); \draw[->] (yR1) -- (t1c);
			
			% --- SETA DE TRANSIÇÃO ---
			\draw[->, ultra thick] (0.5, -1.5) -- (2, -1.5) node[midway, above, align=center, scale=0.8] {Isomorfismo\\de Terminais};
			
			% --- LADO DIREITA: APÓS FUSÃO ---
			\node (labelIso) at (4.5, 0.8) {\textbf{2. OBDD com Terminais Fundidos}};
			\node[v] (x2) at (4.5,0) {$x$};
			\node[v] (yL2) at (3.5,-1.5) {$y$};
			\node[v] (yR2) at (5.5,-1.5) {$y$};
			
			% Terminais únicos (Isomorfismo aplicado)
			\node[t] (T0) at (3.5,-3.5) {0};
			\node[t] (T1) at (5.5,-3.5) {1};
			
			\draw[->, dashed] (x2) -- (yL2); \draw[->] (x2) -- (yR2);
			
			% y da esquerda aponta para 0 e 1
			\draw[->, dashed] (yL2) -- (T0);
			\draw[->] (yL2) -- (T1);
			
			% y da direita aponta apenas para o terminal 1 (mesmo ID)
			\draw[->, dashed] (yR2) -- (T1);
			\draw[->] (yR2) -- (T1);
			
		\end{tikzpicture}
	\end{center}
	
	\textbf{Passo 2: Regra de Eliminação (Nós Redundantes)} \\
	Identificamos o nó $y$ no ramo onde $x=1$. Como $succ_0(y) = succ_1(y) = 1$, o nó $y$ é redundante e deve ser removido, redirecionando o arco vindo de $x$ diretamente para o terminal $1$.
	
	\begin{center}
		\begin{tikzpicture}[node distance=1.5cm, v/.style={circle,draw,minimum size=8mm}, t/.style={rectangle,draw,minimum size=6mm}, >=stealth, thick]
			% Antes
			\node (label1) at (0, 0.8) {\textbf{Antes}};
			\node[v] (x1) at (0,0) {$x$};
			\node[v] (yL1) at (-1,-1.5) {$y$};
			\node[v] (yR1) at (1,-1.5) {$y$};
			\node[t] (t01) at (-1.5,-3) {0};
			\node[t] (t11) at (0.5,-3) {1};
			
			\draw[->, dashed] (x1) -- (yL1); \draw[->] (x1) -- (yR1);
			\draw[->, dashed] (yL1) -- (t01); \draw[->] (yL1) -- (t11);
			\draw[->, dashed] (yR1) -- (t11); \draw[->] (yR1) -- (t11);
			
			% Seta de transformação
			\draw[->, ultra thick] (2, -1.5) -- (3.5, -1.5) node[midway, above] {Redução};
			
			% Depois (ROBDD)
			\node (label2) at (5, 0.8) {\textbf{Depois (ROBDD)}};
			\node[v] (x2) at (5,0) {$x$};
			\node[v] (y2) at (4,-1.5) {$y$};
			\node[t] (t02) at (4,-3) {0};
			\node[t] (t12) at (6,-3) {1};
			
			\draw[->, dashed] (x2) -- (y2);
			\draw[->] (x2) -- (t12); % Redirecionado e reduzido
			\draw[->, dashed] (y2) -- (t02);
			\draw[->] (y2) -- (t12);
		\end{tikzpicture}
	\end{center}
	
	\section{Shared Ordered Binary Decision Diagram (SOBDD)}
	Cada função possui algum ROBDD para ser representada, porém, manter diversos ROBDDs pode ser custoso, pois há redundâncias e e duplicação de funções. Para evitar isto, há o SOBDDS, um OBDD com diversas raízes (um conjunto de raízes ao invés de um único $v_0$). Cada função é representada por uma raiz e as diferentes funções compartilham nós de funções isomórficas.
	
	\subsection*{Exemplo: Shared OBDD (SOBDD)}
	Neste exemplo, as funções $f = x \land y$ e $g = z \lor (x \land y)$ são representadas simultaneamente. A subexpressão $(x \land y)$ é calculada e armazenada apenas uma vez, sendo compartilhada por ambas as raízes.
	
	\begin{center}
		\begin{tikzpicture}[
			node distance=1.5cm and 2cm,
			v/.style={circle, draw, minimum size=8mm, thick},
			t/.style={rectangle, draw, minimum size=6mm, thick},
			root/.style={draw, stealth-, thick, red!70!black},
			>=stealth,
			thick
			]
			
			% Nós da estrutura
			\node[v] (z) {$z$};
			\node[v] (x) [below left=of z, xshift=1cm] {$x$};
			\node[v] (y) [below right=of x, xshift=-1cm] {$y$};
			
			% Terminais únicos
			\node[t] (t0) [below left=1.5cm of y] {0};
			\node[t] (t1) [below right=1.5cm of y] {1};
			
			% Definição das Raízes (Entry Points)
			\draw[root] (z.north) -- ++(0,0.7) node[above] {Raiz de $g$};
			\draw[root] (x.north west) -- ++(-0.5,0.5) node[left] {Raiz de $f$};
			
			% Arestas de Z (Função g)
			\draw[->] (z) -- (t1) node[midway, right, scale=0.8] {1}; % Se z=1, g=1
			\draw[->, dashed] (z) -- (x) node[midway, left, scale=0.8] {0}; % Se z=0, g depende de (x and y)
			
			% Arestas de X (Compartilhadas por f e g)
			\draw[->] (x) -- (y) node[midway, right, scale=0.8] {1};
			\draw[->, dashed] (x) -- (t0);
			
			% Arestas de Y (Compartilhadas)
			\draw[->] (y) -- (t1);
			\draw[->, dashed] (y) -- (t0);
			
		\end{tikzpicture}
	\end{center}
	
	
	\chapter{Mapeamento de Modelos SSMV para BDDs}
	Para converter um modelo SSMV para a representação simbólica, seguimos os seguintes passos:
	
	
	\section{Codificação de Variáveis}
	Variáveis de domínios finitos são mapeadas para vetores de bits boolianos de acordo com o tamanho do domínio do tipo de dado. Seja $len(x)$ o domínio de uma variável do ssmv (inteiro, booliana ou enumeração). A quantidade de variáveis (boolianas do BDD) necessárias para representar cada variável do ssmv é igual a $\lceil \log_2(len(x))\rceil$. 
	
	\subsection*{Exemplo}
	Caso $n: 0..7$, teremos $len(n) = 8$, assim $\lceil \log_2(len(n))\rceil = \lceil \log_2(8)\rceil = 3$, ou seja, precisaremos de 3 variáveis para representar a variável $n$ do ssmv.
	
	\section{Codificação de expressões boolianas}
	Para cada expressão booliana em ssmv, basta compilar as subexpressões para um BDD e realizar a respectiva operação booliana. Seja $f_a$ a função representada pela compilação da expressão ssmv $a$ para um BDD.
	\begin{itemize}
		\item \textbf{a \& b}: $f_{\text{a\&b}} = f_a\land f_b$.
		\item \textbf{a | b}: $f_{\text{a|b}} = f_a\lor f_b$.
		\item \textbf{!a}: $f_{\text{!a}} = \neg f_a$.
		\item \textbf{a->b}: $f_{\text{a->b}} = f_a\to f_b$.
	\end{itemize}
	
	\section{Codificação de expressões relacionais}
	Seja $n = len(a) = len(b)$, $x_i$ são as variáveis codificadas de \textbf{a} e  $y_i$ são as variáveis codificadas de \textbf{b}.
	\begin{itemize}
		\item \textbf{a = b}: $f_{\text{a=b}} =  \bigwedge_{i=1}^n (x_i \leftrightarrow y_i)$
		\item \textbf{a > b}: $f_{\text{a>b}} = \bigvee_{i=1}^n \left( (x_i \land \neg y_i) \land \bigwedge_{j=i+1}^n (x_j \leftrightarrow y_j) \right)$
		\item \textbf{a>=b}: $f_{\text{a>=b}} = f_{\text{a=b}}\lor f_{\text{a>b}} $
		\item \textbf{a<=b}: $f_{\text{a<=b}} = \neg f_{\text{a>b}} $
		\item \textbf{a<b}: $f_{\text{a<b}} = \neg f_{\text{a>=b}} $
	\end{itemize}
	
	\section{Codificação de Operações Aritméticas}
	A aritmética de vetores de bits é implementada simulando circuitos combinatórios através de operações lógicas em BDDs. 
	
	\subsection*{Adição (Ripple Carry Adder)}
	A soma de dois vetores de bits $\textbf{a}$ e $\textbf{b}$ de tamanho $n$ é realizada bit a bit, propagando um bit de transporte (\textit{carry}). O bit de transporte inicial é definido como $c_0 = 0$. Para cada bit $i \in \{1, \dots, n\}$, o bit de soma $s_i$ e o próximo \textit{carry} $c_{i}$ são calculados como:
	\begin{itemize}
		\item $s_i = x_i \oplus y_i \oplus c_{i-1}$
		\item $c_i = (x_i \land y_i) \lor ((x_i \oplus y_i) \land c_{i-1})$
	\end{itemize}
	
	\subsection*{Subtração (Complemento de Dois)}
	A subtração $\textbf{a} - \textbf{b}$ é convertida em uma operação de soma utilizando a representação de complemento de dois. Matematicamente, a operação é equivalente a $\textbf{a} + \neg \textbf{b} + 1$. 
	\begin{itemize}
		\item O \textit{carry}inicial é definido como $c_0 = 1$, representando o $+1$ da fórmula.
		\item $sub_i = x_i \oplus \neg y_i \oplus c_{i-1}$.
	\end{itemize}
	
	\section{Relações de atribuição}
	\subsection*{Conjuntos}
	Seja um conjunto $\{e_1,e_2,...,e_k\}$, $f_{x'}=e_i$ a função que atribui $e_i$ a variável futura $x'$.
	\begin{equation}
		f_{enum} = \bigvee_{i=1}^k f_{x'=e_i}
	\end{equation}
	
	\subsection*{Condicionais}
	A expressão \textbf{\textit{cond}} do ssmv é basciamente um \textit{if-then-else} encadeado, assim, pode ser representado pela operação $ITE(x,y,z)$ de BDDs.
	\begin{equation}
		f_{case} = ITE(c_1, f_{x'=r_1}, ITE(c_2, f_{x'=r_2}, \dots ITE(c_n, f_{x'=r_n}, \text{false}) \dots ))
	\end{equation}
	
	\section{Relação de Transição (\texorpdfstring{$\Delta$}{Delta})}
	A relação de transição é codificada como uma fórmula booliana relacionando variáveis do presente ($x$) e do futuro ($x'$).
	\begin{equation}
		\Delta(s, s') = \bigwedge_{x \in Var} \mathcal{R}_x(s, s')
	\end{equation}
	
	Onde $Rv(s,s')$ é o BDD construído para a variável $x$, integrando as regras de:
	\begin{itemize}
		\item \textbf{Atribuição Simples:} $R_x=(x'\leftrightarrow f_x(s))$
		\item \textbf{Conjuntos:} $R_x= \bigvee_{i=1}^k(x'\leftrightarrow e_i)$
		\item \textbf{Condicionais (Case):} $R_v = ITE(c_1,R_{x1},ITE(c_2,R_{x2},…))$
	\end{itemize}
	
	\chapter{Verificação de modelos simbólica}
	\label{verificacao_simbolica}
	\section{Algortimo de derificação simbólica}
	\subsection{Notas de implementação}
	\textbf{OxiDD}: uma biblioteca para \textit{Binary Decision Diagrams} (BDDs) apresentada em \citeonline{husungOxiDDSafeConcurrent2024}.
	
	\subsection{Resultados e Avaliação}
	Os testes foram realizados comparando o SSMV com o NuSMV. Os resultados estão resumidos na Tabela \ref{tab:bench}.
	
	\begin{table}[ht]
		\centering
		\caption{Comparativo de Performance}
		\label{tab:bench}
		\begin{tabular}{@{}lccc@{}}
			\toprule
			\textbf{Ferramenta} & \textbf{Tempo (ms)} & \textbf{Nós BDD} & \textbf{Memória (MB)} \\ \midrule
			SSMV (BDD)          & 1.918               & 1.815.465        & 90,3                  \\
			NuSMV               & 6.998               & 482.151          & 65,6                  \\ \bottomrule
		\end{tabular}
	\end{table}
	
	\section{Verificação simbólica com reordenamento de variáveis estático}
	Foram usados o ordenamento topológico (ordem de declaração do arquivo .smv), aleatório, heurística FORCE de \citeonline{aloulFORCEFastEasytoimplement2003} e heurística \textit{sifting} produzida pelo NuSMV.
	\subsection{Notas de implementação}
	\subsection{Resultados e Avaliação}
	
	

	

	
	\chapter{Conclusão}
	Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
	
	Trabalhos futuros:
	\begin{itemize}
		\item Restrição de \textit{fairness}.
		\item Geração de contraexemplos.
		\item Mudança para ADD (\textit{Algebraic Decision Diagrams} / MTBDD) para lidar com estruturas numéricas.
		\item Reordenamento dinâmico de variáveis (aguardar atualização da biblioteca OxiDD).
		\item Expansão para LTL.
		\item Comparação de desempenho com outros verificadores.
		\item Migração da linguagem de entrada para MoXI.
	\end{itemize}
	
	\bibliographystyle{abntex2-alf}
	\bibliography{references}
	
\end{document}